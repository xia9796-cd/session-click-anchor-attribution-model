name: demo_project
version: 1.0
config-version: 2

model-paths: ["models"]

models:
  demo_project:
    stg:
      +materialized: view
    int:
      +materialized: view
    mart:
      +materialized: table
